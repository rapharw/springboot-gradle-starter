name: Java CI with Gradle
on:
  push:
    branches:
      - main
      - develop

  pull_request:
    branches:
      - main
      - develop

jobs:
  springboot-gradle:
    uses: rapharw/github-wf-templates/.github/workflows/springboot-gradle.yaml@v0.0.4-alpha