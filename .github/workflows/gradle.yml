name: Java CI with Gradle
on:
  push:
    branches:
      - main
      - develop

  pull_request:
    branches:
      - main
      - develop

jobs:
  run-java-gradle:
    # USER_OR_ORG_NAME/REPO_NAME/.github/workflows/REUSABLE_WORKFLOW_FILE.yml@TAG_OR_BRANCH
    uses: rapharw/github-wf-templates/.github/workflows/main.yml@v0.0.2-alpha